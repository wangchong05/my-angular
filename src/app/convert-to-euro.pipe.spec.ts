import { ConvertToEuroPipe } from './convert-to-euro.pipe';

describe('ConvertToEuroPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToEuroPipe();
    expect(pipe).toBeTruthy();
  });
});
